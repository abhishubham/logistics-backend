{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "node_modules"],
  "exec": "ts-node-dev --respawn --transpile-only -r tsconfig-paths/register src/main.ts",
  "env": {
    "NODE_ENV": "development"
  }
}

