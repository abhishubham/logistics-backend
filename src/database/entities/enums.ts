export enum PartyType {
  CONSIGNEE = 'consignee',
  SHIPPER = 'shipper',
  CARRIER = 'carrier',
  VENDOR = 'vendor',
}

export enum PortOrAirportType {
  PORT = 'port',
  AIRPORT = 'airport',
}

export enum CarrierType {
  AIRLINE = 'airline',
  SHIPPING = 'shipping',
}


